# Duplicate-organizer
This program gets the files with the same name regardless of their extensions (eg. jpd, exe, cpp) and creates a folder putting them in it. It creates a folder for every file in the folder.
